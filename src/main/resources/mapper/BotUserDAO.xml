<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.delmark.dads.notifications.data.repository.BotUserDAO">

    <select id="findById"
            resultType="ru.delmark.dads.notifications.data.model.BotUser">
        SELECT
            u.id,
            u.username,
            am.name as access_level,
            u.created_at
        FROM bot_user u
        JOIN access_mode am ON u.access_level = am.access_level
        WHERE u.id = #{userId}
    </select>

    <insert id="save">
        INSERT INTO bot_user (id, username, access_level)
        VALUES (#{id}, #{username}, #{accessLevel.level})
    </insert>

    <update id="updateUserAccess">
        UPDATE bot_user
        SET access_level = #{accessLevel.level}
        WHERE id = #{userId}
    </update>
</mapper>